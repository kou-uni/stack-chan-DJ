# ROADMAP（2026-09-08 引き直し）

- 今日：**2026-09-08（火）**
- イベント：Crypto Cafe and Bar / ハンズオン形式
- 開催日：**2026-09-29（火）確定** ＝ 残り21日
- 手順の中身は [build-steps.md](docs/build-steps.md)。ここは**順番と日程とカットライン**。

---

## 現在地

**済んだこと**

- 出荷時ファームで実測し、基準を取った（応答速度・騒音耐性・音量・アイドル挙動）
- 設計判断がほぼ固まった
  - 近いMacがゲートウェイ、遠いMacが頭脳（リレー構成）
  - `websocket.url` は空＝mDNS、fallback は自宅Funnel
  - 名乗るかどうかは Mac Studio が自動判定（primary / standby）
  - 当日は MacBook を 2.4GHz 親APにする
  - アバター表示・監視カメラは捨てる

**まだ何も動いていないこと**

- Tailscale **未インストール**
- gateway 未起動 / ファーム未書き換え / サーバー未構築
- MIDI を1バイトも読んでいない

---

## ★順番を入れ替える（提案）

当初は「会話 → DJ」の順だった。**これを逆にする。**

| | 理由 |
|---|---|
| **Bar で音声会話は環境的に一番弱い** | 騒がしい、順番待ちが発生する、遅延が目立つ |
| **DJと踊るのは会場文脈に完全に合う** | DJ台がある場所で、説明ゼロで伝わる |
| **会話には未解決のブロッカーがある** | 公式イメージが x86 のみ。ここで詰まると全部止まる |

会話を最後に置けば、**詰まってもイベントは成立する**。
逆に踊りを最後に置くと、詰まったときに一番強い見せ場を失う。

---

# フェーズ

## Phase 0　並走・今日から（実機もサーバーも不要）

- [ ] `app/dj/midi_dump.py` — DDJ-FLX2 の MIDI をただ表示する
- [ ] **rekordbox が掴んでいる最中に同時に読めるか**を実測 ← 未確認事項がここで消える
- **DoD**：つまみを捻ると、どのCC番号が 0〜127 で動くか一覧できた

## Phase 1　9/8–9/13　身体が動く（D-21 〜 D-16）　★2026-09-08 完了

- [ ] Tailscale を Mac Studio に入れる
- [ ] 出荷時ファームをバックアップ（`esptool read_flash`）
- [ ] gateway を Mac Studio で起動（Python のみ。**Apple Silicon 問題は起きない**）
- [ ] **アンバインド**してから焼く（忘れるとペアリング不整合）
- [ ] ★ **mDNS で見つかるか実測**（`websocket.url` を空に）← 最優先
- [ ] ★ **fallback の挙動を実測**（何秒で切替 / 自動で戻るか）
- [ ] ★ **テザリング10分テスト** — 「2.4GHzに乗るか」だけ確認してすぐ戻す
- **DoD**：**自分のコードで首が回り、写真が撮れた** ← ✅ **達成（9/8）**
      首5方向・表情5種・LED12個・カメラJPEG すべて実機で確認。`tools=40`
      詰まったのは**OTA関門（6桁コード）**。`scripts/ota_stub.py` で突破
- ⚠️ ここから会話は Phase 4 まで戻らない

## Phase 2　9/14–9/18　遠隔で操作できる（D-15 〜 D-11）

- [ ] Web操作パネルを Mac に立てる（FastAPI + HTML 1枚）
- [ ] Funnel で公開（`--https=443`）
- [ ] ★ **本番形テスト** — モバイル回線のスマホ → Funnel → テザリングの本体
- [ ] 遠隔ワークベンチ（Webカメラ / スマートプラグ / `keep-awake.sh`）
- [ ] **外から焼き直して戻すのを1回通す** ← これ以降リモートで進められる
- **DoD**：**外出先のスマホから首が回る**
- ★ ここで本番ネットワークの検証が終わる。**会話より前に済ませるのが肝**

## Phase 3　9/19–9/24　DJと踊る（D-10 〜 D-5）★見せ場　→ **2026-09-08 に前倒しで完了**

- [ ] つまみ → 首（CC 0〜127 を角度に線形マッピング）
  - **gateway の WebSocket を直接叩く。MCP経由にしない**
  - 間引き 30〜50ms、デッドバンド必須
- [ ] PLAY・パッド → 笑う（`set_avatar` / `set_mouth`）
- [ ] **曲間 → 眠そう**（落差がないと置物に見える）
- [ ] リズム v0 — PLAY を1拍目、BPM手入力、**2拍に1回**、先読みオフセット
- [ ] 余裕があれば v1 — Ableton Link（rekordbox は Performance モードのみ）
- **DoD**：**曲を流すと、リズムに乗って踊り、笑う** ← ✅ **達成（9/8）**
      つまみ→首／パッド→表情4種／音楽で踊る／LED12個を4模様7色で拍同期／
      曲の終わりで止まる（「聴き入る」演出つき）
      最大の詰まりは**ロボットが自分のサーボ音を聞いて踊り続けた**こと
      （首が拍に同期して動くので、サーボ音が理想的な「拍」として検出されていた。
       4秒ごとに首を止めて測る「聴き入る」演出で解決）
      モードは OFF/DJ の2つ。PLAY(ch0 #11) でON、モードパッド(ch7 #0)で切替、5分無音で自動OFF

## Phase 4　9/25–9/27　会話を自宅に戻す（D-4 〜 D-2）

- [ ] xiaozhi-esp32-server を **ローカル実行**で動かす（Docker を使わない）
- [ ] Ollama を繋ぐ（`llama3.2:3b` から）→ 宅内で会話 ★第一関門
- [ ] Funnel 経由で外から会話 ★第二関門
- [ ] **相槌を先に返す** / **アイドルを止める** / 人格 + 感情タグ
- **DoD**：**外から会話でき、出荷時と同条件で測って許せる速度差**
- ⚠️ **ここが落ちてもイベントは成立する。** 落ちたら出荷時ファームの会話で代替

## Phase 5　9/28–9/29　仕上げ（D-1 〜 D）

- [ ] ペルソナボード（Phase 2 の操作パネルを拡張するだけ）
- [ ] 通しリハ1回（テザリング・音量・照明・騒音）
- [ ] 持ち物チェック → [event/checklist.md](event/checklist.md)
- [ ] プライバシーの口上 → [event/privacy-notice.md](event/privacy-notice.md)

---

## カットライン

間に合わなかったら、下から捨てる。

| 優先 | 中身 | 落ちたときの代替 |
|---|---|---|
| 1（必須） | **DJと踊る・笑う** | 代替なし。これが無いと会場文脈が消える |
| 2（必須） | **スマホから触れる** | 代替なし。ハンズオンの参加要素そのもの |
| 3 | 自宅LLMとの会話 | **出荷時ファームに戻して会話する**（プライバシーの話は逆に濃くなる） |
| 4 | Ableton Link 同期（v1） | BPM手入力の v0 で十分見える |
| 5 | ペルソナボード | 進行役が手元で人格を差し替える |

**1 と 2 が Phase 2 の終わり（9/18）に揃う。** そこから先は全部おまけ、という設計にしてある。

---

## 落ちうる場所

| リスク | いつ分かる | 手当て |
|---|---|---|
| mDNS が効かない | Phase 1 | 明示URL + fallback に戻す。家では Funnel ヘアピンで遅くなるのを許容 |
| fallback が自動で戻らない | Phase 1 | RST 短押しで手動切替。運用で回す |
| **公式イメージが Apple Silicon で動かない** | Phase 4 | ローカル実行。エミュは STT が重く締切に危険 |
| rekordbox が MIDI を占有する | **Phase 0（今日）** | 音からのビート検出に切り替え |
| 出荷時より遅く感じられる | Phase 4 | 3b／**相槌を先に返す**／ストリーミング |
| 待機中のキョロキョロが不気味 | 実測済 | 待機は動かさない。動くなら理由とセット |
| サーボの発熱・摩耗 | Phase 3 | 2拍に1回から。曲間は休ませる |
| 遠隔で文鎮化して手が届かない | Phase 2 以降 | スマートプラグで電源断。**先にバックアップ** |
| **DJ機材が MacBook 単一障害点** | 当日 | MacBook が落ちたら踊りは戻らない。**会話に寄せる切り返しを用意** |

---

## 各フェーズの終わりにやること

**学びを [docs/learnings.md](docs/learnings.md) に書き足す。**
当日のテキストの素材になる。フェーズの区切りと、詰まって抜けたときに必ず更新する。

## ★2台構成（忘れやすいので先頭近くに置く）

**当日 DJ を動かすのは MacBook。** DJ機材が USB なので、console は
DDJ-FLX2 が刺さっている Mac でしか動かない。

- [ ] **MacBook で一から立ち上げる**（`./scripts/setup.sh` → `service.sh install`）
- [ ] **mDNS の切り替えを実測** — 片方の gateway を落として、残った方に繋がるか。所要秒数
- [ ] 実機の `websocket.url` が空（mDNS 探索）になっているか確認

詳細は [docs/architecture.md](docs/architecture.md) §3.5 配置。

## ファイナライズ時のタスク

**表情が確定してから、最後にまとめてやる。** それまでは起動時の自動ロードで運用する。

- [ ] **表情をファームに焼き込む**（電源だけで自分の顔が出るようにする）
      - いまは `load_avatar_set` で PSRAM に載せている。**電源を切ると既定の顔に戻る**
      - 焼き込むと消えない。ただし表情を変えるたびに再ビルド＋書き込みで20〜30分かかる
      - **だから表情が固まるまでやらない。** いま焼くと作り直しのたびに30分溶ける
      - 手順:
        1. ファームのソースを取得（手元は `firmware/` にビルド済みバイナリのみ）
        2. 14枚のPNGを `~/.stackchan/avatar/` へ置く
           （表情6 `idle/happy/thinking/sad/surprised/embarrassed`、
             目3 `eyes_open/half/closed`、口5 `mouth_closed/half/open/e/u`）
        3. `cd firmware && python scripts/avatar_convert/convert_avatars.py`
           → `main/boards/stackchan/avatar_images.local.cc` ができる（git 管理外）
        4. `firmware/build/` を消してから再ビルド（CMake が local を拾い直すため）
        5. 書き込み前に **NVS を含めた現状のバックアップを取る**
           （2026-09-08、NVS の残留で mDNS が死んで半日溶かした）
      - 焼いたあとも `--verify` を必ず通す
      - 詳細: [docs/learnings.md](docs/learnings.md)「表情が電源で消える件」

- [ ] 焼き込み後、`config.toml` の `[avatar]` を無効化するか判断する
      （焼き込んでも自動ロードは無害。**保険として残す方が安全**かもしれない）

## 未決事項

- [ ] 会場に電源が取れる席があるか
- [ ] 当日 MacBook を持ち込めるか（＝踊りが成立するか）
- [ ] 開始時刻と持ち時間（進行の分数配分が決まらない）
