# stackchan-lab

自宅の Mac Studio を頭脳にして、外に持ち出したスタックチャンをネット越しに動かす。
成果を **Crypto Cafe and Bar のハンズオンイベント（2026-09-29 火）** で使う。

## ★当日のことを考えるときは、先に event/goal.md を読む

**アジェンダ・配布物・台本を触る前に必ず。** ここから逆算する。

帰り道に思っていてほしいこと:
**私もやってみたい／あー聞いておいてよかった／家に帰ったらこれをしよう／
またこの人のイベントに来たい／こんなんまでもらってしまってラッキー**

> 2026-09-15 の失敗：**既にある文書を読まずに、背骨を3回作り直した。**
> 進行表が2つある（`event/run-of-show.md` と `event/text.md`）ことにも気づいていなかった。
> **整備した文書があるなら、まずそれを読む。**

## この機械が MacBook なら、まず立ち上げ

**当日は MacBook に全部載る**（gateway・console・背景・VOICEVOX・Ollama）。
新しい機械で最初にやることは1つ。

```bash
./scripts/bootstrap.sh
```

手で運ぶものは無い。止まったら理由が日本語で出る。詳細は
[docs/macbook-setup.md](docs/macbook-setup.md)。

**vendor/ に手を入れたら `./scripts/pack-patches.sh` を走らせて commit すること。**
改変は vendor/ に置いたままだと、別の機械で復元できない。

## このリポジトリで作業するときの決まり

### ★★ 作る前に、テストを書く

**仕様はテストで固定してから実装する。順番を逆にしない。**

```
① 仕様をテストに書く   「優先順は 喋る > 聞く > 踊り > 消灯」
② 赤を確認する         いま何が満たされていないかを機械に出させる
③ 実装して緑にする
```

**なぜこの順番か**

- **仕様が言葉のままだと、実装しながら都合よくずれる。**テストなら、ずれたら落ちる
- **赤を見ると、直す範囲が確定する。**推測で広く触らなくて済む
- 先に書くと**「何ができれば完成か」が実装前に決まる。**終わりが見える

**実績（2026-09-09〜10）**

- 設計の不変条件7つをテストにしたら、**違反35箇所が機械的に出た**
- 振り付けの可動範囲をテストにしたら、**頷きが40度で潰れていた**のが見つかった
- LEDの色の意味をテストにしたら、**会話中は光らない作りになっていた**のが実装前に分かった

**手で探すな。落ちるようにしておけ。**

### ★★ 出す前に検算する。指摘されてからでは遅い

**「見てください」と言う前に、機械で確かめる。**

```bash
node tests/js/render_check.js app/dj/stage.html   # 落ちずに描けるか
node tests/js/geom_check.js   app/dj/stage.html   # 幾何・明るさ・重なり
./.venv/bin/python -m pytest tests/ -q            # 全体
./.venv/bin/python scripts/check_mapping.py       # 実機の割り当て
```

**背景（`app/dj/stage.html`）を触ったら、必ず一度は撮って見る。**

```bash
scripts/shot.sh /tmp/stage.png       # ヘッドレスChromeで実際の画面を撮る
```

> 2026-09-13：テストは363件すべて緑なのに、**画面はテープの光で洗い流されていた。**
> 加算合成の合計が 2.24 になっていて、芯が白く飛び、会場全体が滲んでいた。
>
> **「描画OK」「幾何OK」は、落ちずに描けたことしか言っていない。**
> 見れば一発で分かる壊れ方が、数字の検算をすり抜ける。

**手で確かめたことは、その場で検算に足す。**
一度でも手で測ったなら、次からは機械が測る。

> 本人の指摘（2026-09-13）：**「お前なんで言われるまでやらないの？」**
>
> 数字で確かめられることを、人に見つけさせていた。
> **HTTP 200 と「落ちずに描けた」を、確認だと思っていた。**

### ★ 学びは指示されなくても書き足す

**[docs/learnings.md](docs/learnings.md) を、指示を待たずに更新すること。**
イベント当日に話すテキストの素材にするため。作業ログではなく「何が言えるか」を書く。

**更新するタイミング（この条件に当てはまったら、その場で書く）**

- フェーズが1つ終わったとき
- **詰まって抜けたとき** ← ここが一番濃い。忘れる前に書く
- 一次情報（公式ドキュメント・実機）で前提が覆ったとき
- 実測値が出たとき（速度・可動域・バッテリー・遅延）

**4分類に振り分ける**

| 分類 | 中身 |
|---|---|
| ベストプラクティス | 他の仕事にも効く一般化。スタックチャン固有はここに置かない |
| アンチパターン | **必ず「なぜそうしたくなるか」とセットで。** 理由のない禁止は守られない |
| ノウハウ | この機材・この構成に固有の知識 |
| **当日のテキストに使えるネタ** | そのまま喋れる形で書く。これが最終目的 |

### ★ HTML でも出す（Artifact）

**節目のドキュメントは Artifact でも公開する。** 本人の希望。

ただし **Markdown の逐語コピーを作らない**（内容が必ずずれる）。
HTML は**視点の違う成果物**にする。

| | 役割 |
|---|---|
| Markdown（`docs/*.md`） | 一次。作業用。詳細と経緯 |
| HTML（Artifact） | **手を動かしながら見るランブック**、または図で見せる構成図 |

出すタイミング：**フェーズの節目**、**構成や手順が固まったとき**、**図で見た方が速いとき**。
既存の Artifact を更新するときは、同じファイルパスで再 publish する（URLが保たれる）。

**公開済み**

| Artifact | URL |
|---|---|
| スタックチャン接続マップ（4シナリオ・構成図） | `scratchpad/stackchan-connection-map.html` |
| スタックチャン書き込み手順（Phase 1 ランブック） | `scratchpad/stackchan-flashing-runbook.html` |

### 一次資料を複製しない

`docs/00-concept.md` が一次資料。要約を別ファイルに切らない（内容がずれる）。
索引と差分メモは `docs/README.md` に置く。

### 判断が動いたら3箇所を揃える

決定・前提・未解決の問いが動いたら、**取り残しを作らない**。

1. `ROADMAP.md`（日程とカットライン）
2. 関連する `docs/*.md`
3. Obsidian の `interests/stackchan-robot-app` ハブと該当ノート

### 締切を意識する

**2026-09-29 が動かない。** 構成の綺麗さより「当日そこで動くこと」を優先する。
迷ったら `ROADMAP.md` のカットラインを見る。
